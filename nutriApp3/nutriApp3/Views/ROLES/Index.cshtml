@model IEnumerable<nutriApp3.Models.ROLES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            NOMBRE
        </th>
        <th>
            USUARIOS
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ROL }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ROL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ROL })
        </td>
        <td>
			@item.NOMBRE
        </td>
        <td>
			@(item.USUARIOS == null ? "None" : item.USUARIOS.Count.ToString())
        </td>
    </tr>
}

</table>


