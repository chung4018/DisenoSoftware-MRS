@model nutriApp3.Models.BITACORA_USUARIOS

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>BITACORA_USUARIOS</legend>

    <div class="display-label">ID_USR_IMPLICADO</div>
    <div class="display-field">@Model.ID_USR_IMPLICADO</div>

    <div class="display-label">CORREO_ANTERIOR</div>
    <div class="display-field">@Model.CORREO_ANTERIOR</div>

    <div class="display-label">NOMBRE_ANTERIOR</div>
    <div class="display-field">@Model.NOMBRE_ANTERIOR</div>

    <div class="display-label">APELLIDO_ANTERIOR</div>
    <div class="display-field">@Model.APELLIDO_ANTERIOR</div>

    <div class="display-label">ID_ROL_ANTERIOR</div>
    <div class="display-field">@String.Format("{0:F}", Model.ID_ROL_ANTERIOR)</div>

    <div class="display-label">FECHA_MODIFICACION</div>
    <div class="display-field">@String.Format("{0:g}", Model.FECHA_MODIFICACION)</div>

    <div class="display-label">ACCION_REALIZADA</div>
    <div class="display-field">@Model.ACCION_REALIZADA</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


