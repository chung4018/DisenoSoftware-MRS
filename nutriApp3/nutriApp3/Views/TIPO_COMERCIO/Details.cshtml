@model nutriApp3.Models.TIPO_COMERCIO

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>TIPO_COMERCIO</legend>

    <div class="display-label">NOMBRE</div>
    <div class="display-field">@Model.NOMBRE</div>

    <div class="display-label">COMERCIOS</div>
    <div class="display-field">@(Model.COMERCIOS == null ? "None" : Model.COMERCIOS.Count.ToString())</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID_TIPO }) |
    @Html.ActionLink("Back to List", "Index")
</p>


