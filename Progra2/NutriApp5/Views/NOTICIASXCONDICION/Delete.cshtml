@model NutriApp5.Models.NOTICIASXCONDICION

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>NOTICIASXCONDICION</legend>

    <div class="display-label">ID_CONDICION</div>
    <div class="display-field">@String.Format("{0:F}", Model.ID_CONDICION)</div>

    <div class="display-label">ID_NOTICIA</div>
    <div class="display-field">@Model.ID_NOTICIA</div>

    <div class="display-label">CONDICION</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.CONDICION).ToString()</div>

    <div class="display-label">NOTICIAS</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.NOTICIAS).ToString()</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


