@model IEnumerable<NutriApp5.Models.USUARIOS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            NOMBRE
        </th>
        <th>
            APELLIDO
        </th>
        <th>
            CORREO
        </th>
        <th>
            CONTRASENA
        </th>
        <th>
            BUSQUEDAS
        </th>
        <th>
            NOTICIAS
        </th>
        <th>
            ROLESXUSUARIO
        </th>
        <th>
            USUARIOXCONDICION
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_USUARIO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_USUARIO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_USUARIO })
        </td>
        <td>
			@item.NOMBRE
        </td>
        <td>
			@item.APELLIDO
        </td>
        <td>
			@item.CORREO
        </td>
        <td>
			@item.CONTRASENA
        </td>
        <td>
			@(item.BUSQUEDAS == null ? "None" : item.BUSQUEDAS.Count.ToString())
        </td>
        <td>
			@(item.NOTICIAS == null ? "None" : item.NOTICIAS.Count.ToString())
        </td>
        <td>
			@(item.ROLESXUSUARIO == null ? "None" : item.ROLESXUSUARIO.Count.ToString())
        </td>
        <td>
			@(item.USUARIOXCONDICION == null ? "None" : item.USUARIOXCONDICION.Count.ToString())
        </td>
    </tr>
}

</table>


